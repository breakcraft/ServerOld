[proc,special_attack](player_uid $target, int $damage, seq $anim, spotanim $gfx, int $cost)
if (specenergy < $cost) {
    mes("You don't have enough special energy.");
    return;
}
specenergy_set(sub(specenergy, $cost));
anim($anim, 0);
spotanim_pl($gfx, 0, 0);
damage($target, 1, $damage);

